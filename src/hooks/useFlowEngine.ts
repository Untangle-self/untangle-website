/**
 * useFlowEngine — ARCHIVED
 *
 * This was the original scripted multi-step flow engine (screen1 → screen4 → paths A/B/C).
 * It is not wired into the current app. The active flow controller is useFlowController.ts.
 *
 * Kept here as a reference for the intended full-flow architecture (loop, summary, clarity).
 * When the scripted/demo path is re-introduced, this should be rebuilt against the current
 * store schema rather than restored as-is.
 */

export {};
